/**
 * 1. Auto waits
 * 2. waitFor() : this wait is applied on a locator 
 * 3. waitForTimeout(): Dum wait , wherein you will just specify the time period for which the test need to wait 
 * 4. waitForLoadState(): Applied over the page object
 */